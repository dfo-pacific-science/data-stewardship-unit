if command -v devenv >/dev/null 2>&1; then
  # Load direnv helpers that define `use devenv`
  eval "$(devenv direnvrc)"
use devenv
else
  echo "devenv is required; install from https://devenv.sh"
  exit 1
fi
